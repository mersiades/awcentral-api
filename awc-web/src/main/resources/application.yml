# https://github.com/graphql-java-kickstart/graphql-spring-boot/issues/165#issuecomment-469183385
graphql:
  servlet:
    websocket:
      enabled: false

logging:
  level:
    org:
      springframework:
        data:
          mongodb: DEBUG