spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/auth/realms/awc-realm
          jwk-set-uri: http://localhost:8180/auth/realms/awc-realm/protocol/openid-connect/certs
  data:
    mongodb:
      uri: mongodb+srv://${MONGO_DB_USERNAME}:${MONGO_DB_PASSWORD}@awcentral-cluster-1.nnjlp.mongodb.net/dev-awcentral?retryWrites=true&w=majority
#  data:
#    mongodb:
#      port: 27017
#      host: localhost
#      database: mongo-dev